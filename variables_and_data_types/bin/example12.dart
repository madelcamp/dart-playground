void main(List<String> args) {
  // Type promotion is treating a less precise value as a more precise value
  final double hisAge = 30;
  print(hisAge);
  //Demoting operation not allowed
  // final int herAge = 40.5;
}
