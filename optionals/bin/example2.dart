void main(List<String> args) {
  // If you're working just with the value of null and you don't have a
  // datatype for it, dart understands that value to be of type DYNAMIC.
  // DYNAMIC can represent pretty much any other data type

  const someValue = null;
  print(someValue);
}
